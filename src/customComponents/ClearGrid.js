import React from 'react'
import { alpha, styled } from '@mui/material/styles';
import Grid from '@mui/material/Grid';

const ClearGrid = styled(Grid)(({ theme }) => ({
  background: "none",
}));

export default ClearGrid;
